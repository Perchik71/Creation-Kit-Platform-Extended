# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
#
# Authors: yak3d, perchik71
# Data: 07/11/2024
# License: GPLv3

name: MSBuild

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  # Path to the solution file relative to the root of the project.
  SOLUTION_FILE_PATH: .
  # Configuration type to build.
  # You can convert this to a build matrix if you need coverage of multiple configuration types.
  # https://docs.github.com/actions/learn-github-actions/managing-complex-workflows#using-a-build-matrix
  BUILD_CONFIGURATION: Release
  BUILD_NOAVX2_CONFIGURATION: Release-NoAVX2

permissions:
  contents: read

jobs:
  build-master:
    runs-on: windows-2022

    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive
        
      # gets the short sha to append it to the archive name
    - name: Get short SHA
      run: echo "SHORT_SHA=$("${{ github.sha }}".SubString(0, 8))" >> $env:GITHUB_ENV
      
    - name: Add MSBuild to PATH
      uses: microsoft/setup-msbuild@v2

    - name: Build
      working-directory: ${{env.GITHUB_WORKSPACE}}
      # Add additional options to the MSBuild command line here (like platform or verbosity level).
      # See https://docs.microsoft.com/visualstudio/msbuild/msbuild-command-line-reference
      run: msbuild /m /p:Configuration=${{env.BUILD_CONFIGURATION}} ${{env.SOLUTION_FILE_PATH}}

    # SSE

    - name: Make Bundle Directory
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: New-Item -ItemType Directory -Path "ckpe-sse-master-${{env.SHORT_SHA}}"

    - name: Copy SSE CKPE
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Recurse -Path ".\x64" -Include "ckpe_loader.exe", "winhttp.dll", "ckpe.dll", "ckpe.common.dll", "ckpe.skyrimse.dll", "ckpe.pluginapi.dll" | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-sse-master-${{env.SHORT_SHA}}" $_.Name) -Force }

    - name: Copy Others to SSE
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Others -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-sse-master-${{env.SHORT_SHA}}" $_.Name) -Force }

    - name: Copy SSE Database
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Copy-Item -Path Database\SSE\*.pak "ckpe-sse-master-${{env.SHORT_SHA}}"

    - name: Copy SSE Dialog
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Copy-Item -Path Dialogs\SSE\*.pak "ckpe-sse-master-${{env.SHORT_SHA}}"
      
    - name: Copy SSE D3DCompiler
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Copy-Item -Path d3dcompiler\*.dll "ckpe-sse-master-${{env.SHORT_SHA}}"

    - name: Copy SSE Stuff
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Stuffs\SSE -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-sse-master-${{env.SHORT_SHA}}" $_.Name) -Force }
      
    - name: Copy Custom Theme Stuff
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Stuffs\CustomTheme -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-sse-master-${{env.SHORT_SHA}}" $_.Name) -Force }
      
    - name: Copy SSE Vortex Stuff
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Stuffs\Vortex -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-sse-master-${{env.SHORT_SHA}}" $_.Name) -Force }
      
    - name: Upload SSE build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ckpe-sse-master-${{env.SHORT_SHA}}
        path: "ckpe-sse-master-${{env.SHORT_SHA}}"
        
    # FO4

    - name: Make Bundle Directory
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: New-Item -ItemType Directory -Path "ckpe-fo4-master-${{env.SHORT_SHA}}"

    - name: Copy FO4 CKPE
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Recurse -Path ".\x64" -Include "ckpe_loader.exe", "winhttp.dll", "ckpe.dll", "ckpe.common.dll", "ckpe.fallout4.dll", "ckpe.pluginapi.dll" | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-fo4-master-${{env.SHORT_SHA}}" $_.Name) -Force }

    - name: Copy Others to FO4
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Others -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-fo4-master-${{env.SHORT_SHA}}" $_.Name) -Force }

    - name: Copy FO4 Database
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Copy-Item -Path Database\FO4\*.pak "ckpe-fo4-master-${{env.SHORT_SHA}}"

    - name: Copy FO4 Dialog
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Copy-Item -Path Dialogs\FO4\*.pak "ckpe-fo4-master-${{env.SHORT_SHA}}"
      
    - name: Copy FO4 D3DCompiler
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Copy-Item -Path d3dcompiler\*.dll "ckpe-fo4-master-${{env.SHORT_SHA}}"

    - name: Copy FO4 Stuff
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Stuffs\FO4 -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-fo4-master-${{env.SHORT_SHA}}" $_.Name) -Force }
      
    - name: Copy Custom Theme Stuff
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Stuffs\CustomTheme -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-fo4-master-${{env.SHORT_SHA}}" $_.Name) -Force }
      
    - name: Copy FO4 Vortex Stuff
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Stuffs\Vortex -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-fo4-master-${{env.SHORT_SHA}}" $_.Name) -Force }
      
    - name: Upload FO4 build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ckpe-fo4-master-${{env.SHORT_SHA}}
        path: "ckpe-fo4-master-${{env.SHORT_SHA}}"
        
    # SF

    - name: Make Bundle Directory
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: New-Item -ItemType Directory -Path "ckpe-sf-master-${{env.SHORT_SHA}}"

    - name: Copy SF CKPE
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Recurse -Path ".\x64" -Include "ckpe_loader.exe", "winhttp.dll", "ckpe.dll", "ckpe.common.dll", "ckpe.starfield.dll", "ckpe.pluginapi.dll" | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-sf-master-${{env.SHORT_SHA}}" $_.Name) -Force }

    - name: Copy Others to SF
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Others -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-sf-master-${{env.SHORT_SHA}}" $_.Name) -Force }

    - name: Copy SF Database
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Copy-Item -Path Database\SF\*.pak "ckpe-sf-master-${{env.SHORT_SHA}}"

    - name: Copy SF Dialog
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Copy-Item -Path Dialogs\SF\*.pak "ckpe-sf-master-${{env.SHORT_SHA}}"

    - name: Copy SF Stuff
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Stuffs\SF -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-sf-master-${{env.SHORT_SHA}}" $_.Name) -Force }
      
    - name: Copy Custom Theme Stuff
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Stuffs\CustomTheme -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-sf-master-${{env.SHORT_SHA}}" $_.Name) -Force }
      
    - name: Copy SF Vortex Stuff
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Stuffs\Vortex -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-sf-master-${{env.SHORT_SHA}}" $_.Name) -Force }
      
    - name: Upload SF build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ckpe-sf-master-${{env.SHORT_SHA}}
        path: "ckpe-sf-master-${{env.SHORT_SHA}}"
        
  noavx2-build-master:
    runs-on: windows-2022

    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive
        
      # gets the short sha to append it to the archive name
    - name: Get short SHA
      run: echo "SHORT_SHA=$("${{ github.sha }}".SubString(0, 8))" >> $env:GITHUB_ENV
      
    - name: Add MSBuild to PATH
      uses: microsoft/setup-msbuild@v2

    - name: Build
      working-directory: ${{env.GITHUB_WORKSPACE}}
      # Add additional options to the MSBuild command line here (like platform or verbosity level).
      # See https://docs.microsoft.com/visualstudio/msbuild/msbuild-command-line-reference
      run: msbuild /m /p:Configuration=${{env.BUILD_NOAVX2_CONFIGURATION}} ${{env.SOLUTION_FILE_PATH}}

    # SSE

    - name: Make Bundle Directory
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: New-Item -ItemType Directory -Path "ckpe-sse-master-noavx2-${{env.SHORT_SHA}}"

    - name: Copy SSE CKPE
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Recurse -Path ".\x64" -Include "ckpe_loader.exe", "winhttp.dll", "ckpe.dll", "ckpe.common.dll", "ckpe.skyrimse.dll", "ckpe.pluginapi.dll" | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-sse-master-noavx2-${{env.SHORT_SHA}}" $_.Name) -Force }

    - name: Copy Others to SSE
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Others -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-sse-master-noavx2-${{env.SHORT_SHA}}" $_.Name) -Force }

    - name: Copy SSE Database
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Copy-Item -Path Database\SSE\*.pak "ckpe-sse-master-noavx2-${{env.SHORT_SHA}}"

    - name: Copy SSE Dialog
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Copy-Item -Path Dialogs\SSE\*.pak "ckpe-sse-master-noavx2-${{env.SHORT_SHA}}"
      
    - name: Copy SSE D3DCompiler
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Copy-Item -Path d3dcompiler\*.dll "ckpe-sse-master-noavx2-${{env.SHORT_SHA}}"

    - name: Copy SSE Stuff
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Stuffs\SSE -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-sse-master-noavx2-${{env.SHORT_SHA}}" $_.Name) -Force }
      
    - name: Copy Custom Theme Stuff
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Stuffs\CustomTheme -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-sse-master-noavx2-${{env.SHORT_SHA}}" $_.Name) -Force }
      
    - name: Copy SSE Vortex Stuff
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Stuffs\Vortex -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-sse-master-noavx2-${{env.SHORT_SHA}}" $_.Name) -Force }
      
    - name: Upload SSE build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ckpe-sse-master-noavx2-${{env.SHORT_SHA}}
        path: "ckpe-sse-master-noavx2-${{env.SHORT_SHA}}"
        
    # FO4

    - name: Make Bundle Directory
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: New-Item -ItemType Directory -Path "ckpe-fo4-master-noavx2-${{env.SHORT_SHA}}"

    - name: Copy FO4 CKPE
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Recurse -Path ".\x64" -Include "ckpe_loader.exe", "winhttp.dll", "ckpe.dll", "ckpe.common.dll", "ckpe.fallout4.dll", "ckpe.pluginapi.dll" | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-fo4-master-noavx2-${{env.SHORT_SHA}}" $_.Name) -Force }

    - name: Copy Others to FO4
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Others -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-fo4-master-noavx2-${{env.SHORT_SHA}}" $_.Name) -Force }

    - name: Copy FO4 Database
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Copy-Item -Path Database\FO4\*.pak "ckpe-fo4-master-noavx2-${{env.SHORT_SHA}}"

    - name: Copy FO4 Dialog
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Copy-Item -Path Dialogs\FO4\*.pak "ckpe-fo4-master-noavx2-${{env.SHORT_SHA}}"
      
    - name: Copy FO4 D3DCompiler
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Copy-Item -Path d3dcompiler\*.dll "ckpe-fo4-master-noavx2-${{env.SHORT_SHA}}"

    - name: Copy FO4 Stuff
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Stuffs\FO4 -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-fo4-master-noavx2-${{env.SHORT_SHA}}" $_.Name) -Force }
      
    - name: Copy Custom Theme Stuff
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Stuffs\CustomTheme -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-fo4-master-noavx2-${{env.SHORT_SHA}}" $_.Name) -Force }
      
    - name: Copy FO4 Vortex Stuff
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Stuffs\Vortex -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-fo4-master-noavx2-${{env.SHORT_SHA}}" $_.Name) -Force }
      
    - name: Upload FO4 build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ckpe-fo4-master-noavx2-${{env.SHORT_SHA}}
        path: "ckpe-fo4-master-noavx2-${{env.SHORT_SHA}}"
        
    # SF

    - name: Make Bundle Directory
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: New-Item -ItemType Directory -Path "ckpe-sf-master-noavx2-${{env.SHORT_SHA}}"

    - name: Copy SF CKPE
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Recurse -Path ".\x64" -Include "ckpe_loader.exe", "winhttp.dll", "ckpe.dll", "ckpe.common.dll", "ckpe.starfield.dll", "ckpe.pluginapi.dll" | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-sf-master-noavx2-${{env.SHORT_SHA}}" $_.Name) -Force }

    - name: Copy Others to SF
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Others -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-sf-master-noavx2-${{env.SHORT_SHA}}" $_.Name) -Force }

    - name: Copy SF Database
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Copy-Item -Path Database\SF\*.pak "ckpe-sf-master-noavx2-${{env.SHORT_SHA}}"

    - name: Copy SF Dialog
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Copy-Item -Path Dialogs\SF\*.pak "ckpe-sf-master-noavx2-${{env.SHORT_SHA}}"

    - name: Copy SF Stuff
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Stuffs\SF -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-sf-master-noavx2-${{env.SHORT_SHA}}" $_.Name) -Force }
      
    - name: Copy Custom Theme Stuff
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Stuffs\CustomTheme -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-sf-master-noavx2-${{env.SHORT_SHA}}" $_.Name) -Force }
      
    - name: Copy SF Vortex Stuff
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: Get-ChildItem -Path .\Stuffs\Vortex -Recurse | ForEach-Object { Copy-Item -Path $_.FullName -Destination (Join-Path "ckpe-sf-master-noavx2-${{env.SHORT_SHA}}" $_.Name) -Force }
      
    - name: Upload SF build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ckpe-sf-master-noavx2-${{env.SHORT_SHA}}
        path: "ckpe-sf-master-noavx2-${{env.SHORT_SHA}}"